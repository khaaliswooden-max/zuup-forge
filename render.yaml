# ZUUP FORGE — Aureon Platform Deployment
# Deploy: Connect this repo at https://dashboard.render.com and select "Blueprint"
# Root Directory: zuup-forge (if repo root differs)

services:
  - type: web
    name: aureon
    runtime: docker
    dockerfilePath: zuup-forge/platforms/aureon/Dockerfile
    dockerContext: zuup-forge
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
