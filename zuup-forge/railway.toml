# ZUUP FORGE — Aureon Platform Deployment
# Use with Root Directory = . (this folder)
# Set RAILWAY_DOCKERFILE_PATH=platforms/aureon/Dockerfile in Railway dashboard if needed

[build]
builder = "dockerfile"
dockerfilePath = "platforms/aureon/Dockerfile"

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
